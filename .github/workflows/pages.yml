name: Deploy Forward Thinking website

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build and verify brand image
        shell: bash
        run: |
          mkdir -p assets
          base64 --decode assets/brand.b64 > assets/forward-thinking-brand.jpg
          echo "3733bbfa0fcadf769c87ef1c4cea7a1fdebca1d4b7b0996bf415e08da757e987  assets/forward-thinking-brand.jpg" | sha256sum -c -
          file assets/forward-thinking-brand.jpg | grep -q "JPEG image data"
          test -s assets/forward-thinking-brand.jpg
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
